# 사내 IT자산 배정·대여/정산 DB 설계

## 👀목차
1. [👥팀원](#팀원)
2. [📚프로젝트 개요](#프로젝트-개요)  
	2.1 [💡배경](#배경)  
	2.2 [🎯서비스 목표](#서비스-목표)  
3. [📅WBS](#WBS)
4. [📄프로젝트 기획서](#프로젝트-기획서)
5. [🎬프로젝트 시나리오](#프로젝트-시나리오)  
6. [📘요구사항 명세서](#요구사항-명세서)  
7. [🗃️테이블 명세서](#테이블-명세서)
8. [📊ERD](#ERD)
9. [💾SQL](#SQL)
10. [회고록](#회고록)

---

### 👥팀원

| 이름 | GitHub |
|---|---|
| (신민수) | (https://github.com/ZonezIpex) |
| (김승욱) | ([링크](https://github.com/KIM-SUNG-UK)) |
| (김지연) | (링크) |
| (모희주) | (링크) |
| (박지인) | (링크) |
| (윤준상) | (https://github.com/wnstkd704) |

---

### 📚프로젝트 개요
---

#### 💡배경
사내 IT 자산(노트북, 모니터, 태블릿, 모바일 단말 등)은 **프로젝트 투입/부서 이동/외주·협력사 투입** 같은 이유로 배정과 회수가 자주 반복됩니다.  
그런데 실제 운영에서는 대여·반납 기록이 메신저/엑셀/담당자 개인 기록으로 흩어져 **“누가 어떤 자산을 언제부터 쓰는지”**, 
**“반납 검수 결과가 무엇인지”**, **“파손·연체 비용을 어떤 근거로 산정했는지”**를 한 번에 추적하기 어렵습니다.

이 상태가 길어지면 **분실/중복 지급/재고 불일치** 같은 운영 리스크가 커지고, 
검수 기준이 사람마다 달라 **파손·수리비 정산에서 분쟁**이 생깁니다.  
특히 보증금 환급·차감, 연체료 산정이 계약/검수 이력과 분리되면 “정산 근거”가 
약해져 운영 난이도가 급격히 올라갑니다.

본 프로젝트는 **조달(PO→배송→입고→검수) → 자산화 → 대여/반납 → 파손·연체·보증금 정산**까지의 흐름을 
DB 모델로 고정하여, 상태 역전(입고 전 대여 등)과 근거 누락(검수 없이 정산 등)을 예방하는 것을 목표로 합니다.

#### 🎯서비스 목표
- **조달부터 자산화까지** 상태 전이(PO/Shipment/Receiving/Asset) 정합성 확보
- **자산 단위(시리얼/자산태그)** 추적 + 상태 이력(언제/누가/왜) 기록
- **대여/반납/검수**를 분리해 책임 경계와 이력 근거 확보
- **보증금 원장(ledger)** 기반 환급/차감으로 정산 재현 가능하게 설계
- **연체/파손 정산 근거**(정산라인, 정책 버전) 저장으로 분쟁 포인트 방어
- 멀티테넌시 기본값: **company_id 방식**(회사별 데이터 혼합 방지)

---

### 📅WBS
- (링크/파일 경로를 여기에 추가)
  - 예: `./docs/WBS.xlsx` 또는 구글 시트 링크

---

### 📄프로젝트 기획서
- (링크/파일 경로를 여기에 추가)
  - 예: `./docs/기획서.pdf`

---

### 🎬프로젝트 시나리오

#### 1) 역할(액터)
| 역할 | 하는 일(핵심) |
|---|---|
| 고객사 구매담당 | PO 생성/승인/발주 전송 |
| 공급사 담당자 | PO 수락/거절, Shipment 처리 |
| 고객사 자산관리자 | Receiving 등록, 입고 검수, 자산 등록 |
| 렌탈 담당자 | 렌탈 계약, 보증금, 자산할당, 출고/반납 관리 |
| 임차사 담당자 | 대여확인, 반납 요청 |
| 정산/회계 담당자 | 정산서, 보증금 원장반영, 정산 확정 |

#### 2) 시나리오 A: 조달(PO → Shipment → Receiving → Asset)
**정상 흐름(수락 + 검수 합격)**  
1. PO 생성 → PO 승인 → PO 전송/발주  
2. 공급사: PO 수락  
3. 공급사: Shipment 생성 → 운송장 등록 → 배송완료  
4. 고객사: Receiving 등록 → 입고 검수  
5. 검수 합격 → 자산 개별 등록(시리얼/자산태그) → 자산 상태 AVAILABLE

**예외 흐름**  
- PO 거절: 발주 거절 통지 → 종료  
- 검수 불합격(불량): RMA 처리 → 종료(기본)

#### 3) 시나리오 B: 대여/반납 + 보증금/연체/파손 정산
**대여**  
1. 렌탈 계약 생성  
2. 보증금 설정/수납  
3. 자산 할당  
4. 물품 대여/출고  
5. 임차사 인수(대여확인)

**반납/검수**  
6. 임차사 반납 요청  
7. 반납 접수 + 검수  
8. 파손 여부 판단  
9. 연체 여부 판단(연체일수 계산 포함)

**정산(순서 고정)**  
10. 정산서 생성  
11. 보증금 원장 반영(환급/차감)  
12. 정산 확정 → 결과 통지

#### 4) 시나리오에서 자주 깨지는 지점(감점 포인트 방어)
- 출고/인수확인이 섞임 → DB에서 `issued_at`(출고), `confirmed_at`(인수)로 분리
- 반납과 검수가 한 번에 처리됨 → 반납 접수와 검수 결과를 분리(근거 누락 방지)
- 정산 결과만 저장 → `settlement_line`(base/qty/rate)로 **근거 저장**
- 정책 변경 시 과거 정산 재현 불가 → 정책 테이블에 **유효기간(effective_from/to)**로 버전 관리

---

### 📘요구사항 명세서

#### 조달(PO/Shipment/Receiving)
| 코드 | 요구사항 |
|---|---|
| proc-001 | 구매담당은 PO를 생성할 수 있다(DRAFT). |
| proc-002 | 구매담당은 PO를 승인할 수 있다(APPROVED). |
| proc-003 | 승인된 PO만 공급사로 전송할 수 있다(SENT). |
| proc-004 | 공급사는 PO를 수락/거절할 수 있다(ACCEPTED/REJECTED). |
| proc-005 | 수락된 PO에 대해 Shipment를 생성하고 운송장/배송완료 이벤트를 기록한다. |
| proc-006 | 자산관리자는 Receiving을 등록하고, 검수 결과(PASS/FAIL)를 기록한다. |

#### 자산(Asset)
| 코드 | 요구사항 |
|---|---|
| asset-001 | 검수 합격 건에 대해서만 자산을 개별 등록(시리얼/자산태그)할 수 있다. |
| asset-002 | 자산은 상태(REGISTERED/AVAILABLE/ALLOCATED 등)와 상태 이력을 가진다. |

#### 대여/반납/정산
| 코드 | 요구사항 |
|---|---|
| rent-001 | 렌탈 담당자는 렌탈 계약과 계약 라인을 생성할 수 있다. |
| rent-002 | 계약 단위로 보증금을 설정/수납하고 원장에 기록한다. |
| rent-003 | 자산 할당 후 출고(issued_at)를 기록하고, 임차사 인수확인(confirmed_at)을 기록한다. |
| rent-004 | 임차사는 반납 요청을 등록할 수 있다. |
| rent-005 | 반납 접수 후 검수 결과(파손 여부/수리비)를 기록한다. |
| rent-006 | 반납 기한 초과 시 연체일수를 계산하고 연체료를 산정한다(정책 기반). |
| settle-001 | 정산서는 검수 완료 후 생성된다. |
| settle-002 | 정산서에는 정산라인(근거)이 포함된다. |
| settle-003 | 정산서 → 보증금 원장 반영(환급/차감) → 정산 확정 순서를 유지한다. |

---

### 🗃️테이블 명세서
- (링크/파일 경로를 여기에 추가)
  - 예: `./docs/table-spec.xlsx`
- 작성 기준(권장)
  - PK/FK/UNIQUE/INDEX, NOT NULL, 상태코드, 금액/날짜 타입, 삭제전략(soft delete 여부)까지 포함

---

### 📊ERD
- (파일 경로를 여기에 추가)
  - 예: `./diagrams/erd.png` 또는 `./diagrams/erd.drawio`

---

### 💾SQL

> SQL은 README에 길게 붙이지 않고 파일로 관리합니다(리뷰/검증용).  
> - `./sql/schema.sql` : DDL  
> - `./sql/test_data.sql` : 테스트 데이터  
> - `./sql/queries.sql` : 검증 쿼리(흐름 누락/역전 탐지)

<details>
	<summary>검증 쿼리 예시(흐름 역전 탐지)</summary>

```sql
-- [검증] 검수(PASSED) 없이 자산이 등록된 케이스 탐지(예시)
-- 실제 테이블/컬럼명은 팀 DDL에 맞춰 수정
SELECT a.asset_id, a.serial_no, r.receiving_id, r.status AS receiving_status
FROM asset a
LEFT JOIN receiving r
  ON r.receiving_id = a.receiving_id
WHERE r.receiving_id IS NULL
   OR r.status <> 'PASSED';
```
</details>

---

### 회고록
- (작성 예정)
